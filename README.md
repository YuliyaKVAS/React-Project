# React-Project
## A promo site for Barbershop
This project bootstrapped with Create React App using Material UI.
###The following steps are required to run this project:
### *git clone
to install all dependencies:
### * npm install
### * npm start
### Open http://localhost:3000 to view it in the browser.
